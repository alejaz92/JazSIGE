# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Deploy StockService (dev)

on:
  push:
    branches: [ main ]
    paths:
      - 'StockService/**'
      - '.github/workflows/main_stock-api-dev.yml'

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore
        run: dotnet restore ./StockService/StockService.csproj

      - name: Publish (StockService only)
        run: dotnet publish ./StockService/StockService.csproj -c Release -o ${{ runner.temp }}/publish-stock -v minimal

      - name: Sanity check
        run: |
          ls -la ${{ runner.temp }}/publish-stock
          test -f ${{ runner.temp }}/publish-stock/StockService.dll
          if [ -f "${{ runner.temp }}/publish-stock/web.config" ]; then
            grep -q "StockService.dll" ${{ runner.temp }}/publish-stock/web.config
          fi

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: stock-api-dev
          package: ${{ runner.temp }}/publish-stock
