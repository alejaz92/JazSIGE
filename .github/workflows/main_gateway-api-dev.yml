# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Deploy GatewayService (dev)

on:
  push:
    branches: [ main ]
    paths:
      - 'GatewayService/**'
      - '.github/workflows/main_gateway-api-dev.yml'

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore
        run: dotnet restore ./GatewayService/GatewayService.csproj

      - name: Publish (GatewayService only)
        run: dotnet publish ./GatewayService/GatewayService.csproj -c Release -o ${{ runner.temp }}/publish-gateway -v minimal

      - name: Sanity check
        run: |
          ls -la ${{ runner.temp }}/publish-gateway
          test -f ${{ runner.temp }}/publish-gateway/GatewayService.dll
          if [ -f "${{ runner.temp }}/publish-gateway/web.config" ]; then
            grep -q "GatewayService.dll" ${{ runner.temp }}/publish-gateway/web.config
          fi

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: gateway-api-dev
          package: ${{ runner.temp }}/publish-gateway
