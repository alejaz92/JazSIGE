# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Deploy FiscalDocumentationService (dev)

on:
  push:
    branches: [ main ]
    paths:
      - 'FiscalDocumentationService/**'
      - '.github/workflows/main_fiscal-api-dev.yml'

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore
        run: dotnet restore ./FiscalDocumentationService/FiscalDocumentationService.csproj

      - name: Publish (FiscalDocumentationService only)
        run: dotnet publish ./FiscalDocumentationService/FiscalDocumentationService.csproj -c Release -o ${{ runner.temp }}/publish-fiscal -v minimal

      - name: Sanity check
        run: |
          ls -la ${{ runner.temp }}/publish-fiscal
          test -f ${{ runner.temp }}/publish-fiscal/FiscalDocumentationService.dll
          if [ -f "${{ runner.temp }}/publish-fiscal/web.config" ]; then
            grep -q "FiscalDocumentationService.dll" ${{ runner.temp }}/publish-fiscal/web.config
          fi

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: fiscal-api-dev
          package: ${{ runner.temp }}/publish-fiscal
