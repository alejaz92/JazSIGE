# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Deploy AuthService (dev)

on:
  push:
    branches: [ main ]
    paths:
      - 'AuthService/**'
      - '.github/workflows/main_auth-api-dev.yml'

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore
        run: dotnet restore ./AuthService/AuthService.csproj

      - name: Publish (AuthService only)
        run: dotnet publish ./AuthService/AuthService.csproj -c Release -o ${{ runner.temp }}/publish-auth -v minimal

      - name: Sanity check
        run: |
          ls -la ${{ runner.temp }}/publish-auth
          test -f ${{ runner.temp }}/publish-auth/AuthService.dll
          if [ -f "${{ runner.temp }}/publish-auth/web.config" ]; then
            grep -q "AuthService.dll" ${{ runner.temp }}/publish-auth/web.config
          fi

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: auth-api-dev
          package: ${{ runner.temp }}/publish-auth
